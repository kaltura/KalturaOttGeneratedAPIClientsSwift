language: swift
osx_image: xcode8.3
before_script:
  - pod repo update
script:
  - pod lib lint --fail-fast --verbose
notifications:
  email:
    recipients:
      - noam.tamim@kaltura.com
    on_success: change
    on_failure: always
